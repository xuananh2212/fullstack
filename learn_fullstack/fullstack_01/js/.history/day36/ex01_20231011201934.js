/**
 *   Authentication => xác thực
 * => đăng nhập và đăng kí.
 *   Authorization => vai trò , uỷ quyền
 * => được làm gì ?  quyền hạn như thế nào.
 *
 * 1.authentication
 * => 2 loại authentication
 * -session -based authentication
 * - token-based  authentication
 *
 * - access token : có thời gian ngắn. xác thực ứng dụng.
 * and
 * - refresh token: dùng để acess token
 *
 *
 */
